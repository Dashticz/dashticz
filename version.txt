{
    "version": "3.45.7",
    "branch": "beta",
    "last_changes": "Reworked Device Rules Automation to a v2 schema that groups one trigger with two independent, simultaneously-selectable actions instead of a single mutually-exclusive action: CSS styling applied to the current device (as before), and a new \"Set text\" action that writes separate \"text when true\"/\"text when false\" values onto a different, selected target device's title via Dashticz.setBlock. The normaliser still accepts previously saved flat action/target/className and action/target/textOn/textOff records, so existing automation keeps working unchanged. Also added a new \"Floating banner\" styling option (alongside the existing background/border/text combinations), matching the hand-written custom.css pattern for full-screen alert banners - a visibility toggle plus a :before with static content, background, border, border-radius, centered fixed position and z-index; per-rule fields are banner text (baked into the CSS content: value at save time, validated client- and server-side to reject quote/backslash characters since it's written verbatim into a CSS string), distance from top in px (so multiple banners can stack without overlapping), and font size, with width left automatic so the banner fits any length of text. Adjusted the text action to fill the target device's data/value field instead of its title, and added a Text output mode (Replace existing text / Add as a separate line) so several automations can share one target device without overwriting each other - separate-line contributions render as sorted, stacked lines with a multiline-friendly white-space style. A rule's disabled master switch now also suppresses its generated server-side CSS (it previously only stopped client-side application), and a target's live On/Off text is reconciled immediately from the last known source device state on save, instead of waiting for the next Domoticz update. Changed the text action's default output mode from Replace to Separate line, so multiple automation rules that target the same device stack as multiple lines out of the box instead of the last active rule silently overwriting the others; Replace remains available per rule for the old overwrite behavior, and existing saved rules that already have an explicit outputMode are unaffected. Verified with the full node --test suite (191 tests) and Prettier's format check.",
    "changelog": {
        "3.45.7": "Reworked Device Rules Automation to a v2 schema that groups one trigger with two independent, simultaneously-selectable actions instead of a single mutually-exclusive action: CSS styling applied to the current device (as before), and a new \"Set text\" action that writes separate \"text when true\"/\"text when false\" values onto a different, selected target device's title via Dashticz.setBlock. The normaliser still accepts previously saved flat action/target/className and action/target/textOn/textOff records, so existing automation keeps working unchanged. Also added a new \"Floating banner\" styling option (alongside the existing background/border/text combinations), matching the hand-written custom.css pattern for full-screen alert banners - a visibility toggle plus a :before with static content, background, border, border-radius, centered fixed position and z-index; per-rule fields are banner text (baked into the CSS content: value at save time, validated client- and server-side to reject quote/backslash characters since it's written verbatim into a CSS string), distance from top in px (so multiple banners can stack without overlapping), and font size, with width left automatic so the banner fits any length of text. Adjusted the text action to fill the target device's data/value field instead of its title, and added a Text output mode (Replace existing text / Add as a separate line) so several automations can share one target device without overwriting each other - separate-line contributions render as sorted, stacked lines with a multiline-friendly white-space style. A rule's disabled master switch now also suppresses its generated server-side CSS (it previously only stopped client-side application), and a target's live On/Off text is reconciled immediately from the last known source device state on save, instead of waiting for the next Domoticz update. Changed the text action's default output mode from Replace to Separate line, so multiple automation rules that target the same device stack as multiple lines out of the box instead of the last active rule silently overwriting the others; Replace remains available per rule for the old overwrite behavior, and existing saved rules that already have an explicit outputMode are unaffected. Verified with the full node --test suite (191 tests) and Prettier's format check.",
        "3.45.6": "Extended #195's icon-button conversion (previously Device Config only) to every other place Icon/Updated/Title/Data switches appeared: the Screen Editor's five quick-add popups (Custom device, Multi Device, Group, HTML Block, LMS - all built from the shared _quickOptionsHtml()/_wireQuickOptions()/_readQuickOptions() trio in js/deviceeditor.js) and Widget Config's own Icon/Title row (js/widgeteditor.js's _widgetBlockOptionsHtml(), .we-block-options-row/.we-block-option), reached via the same click-to-toggle .active pattern as Device Config's row instead of native checkboxes. button.js's injected Background toggle now matches whichever row it lands in (icon button for both Device Config and Widget Config). Removed the now-fully-unused .de-config-options switch-grid CSS (and its -three/-four/-five column-count modifiers) and the .we-block-option.form-check-input switch-sizing rule, since nothing renders that markup anymore; folded the surviving switches (#hb-device-border, .we-widget-field, #we-cfg-ascending, .de-switch, .de-lms-switch) into the shared blue-switch color rule on their own. Fixed a block's icon rendering visibly larger than every other block's icon under the Modern Dark/Liquid Glass themes when the block has no refresh() of its own to later repaint it through iconORimage() (which always adds the class) - getColIcon() (js/dashticz.js), the one-time initial-render path every block goes through, was missing the 'icon' class its own image branch right below it already had, so blocks that never get repainted (for example an HTML block with a custom icon) permanently rendered via themes' unscoped default size instead of their .col-icon .icon size variable. Verified with the full node --test suite (148 tests), Prettier's format check, and Playwright screenshots of all five quick-add popups' Display options row, Widget Config's, and the icon-class fix rendered live in a real browser. Fixed the Screen Editor quick-add popups' (Custom device, Multi Device, Group, HTML Block, LMS) Icon/Updated/Title row rendering centered instead of left-aligned like Device Config's own row - _quickOptionsHtml() (js/deviceeditor.js) still had justify-content-center left over from the old switch layout. Also left-aligned Device Config's own Icon/Data/Updated/Title/Background group specifically (justify-content-between on its shared row instead of centering the whole Icon-group-plus-Dial/Bar/Slider-group pair), so it now sits at the left edge with Dial/Bar/Slider pinned to the right, matching #195's original mockup instead of only looking left-of-center by coincidence. Fixed 'Display options' showing in English regardless of the active language in Device Config and every quick-add popup - the display_options translation key only ever existed under settings.widgeteditor in lang/*.json, never under settings.deviceeditor, so deviceeditor.js's _translations() always fell through to its own hardcoded English default. Added the key to settings.deviceeditor in the three language files that already translate it for Widget Config (en_US, fr_FR, nl_NL) with the same translated text; every other language already showed this label in English for Widget Config too (no widgeteditor translation of its own), so deviceeditor.js's English default is unchanged behavior for those. Extended #195 to the Slide button quick-add popup, the one place it was still missing entirely - Icon was previously shown unconditionally with no toggle at all, and its 'No background' checkbox (added by button.js's injectButtonEditor(), #171) was a lone switch with no Display options row around it. _showSlideButtonPopup() (js/deviceeditor.js) now renders a real Display options row with an Icon toggle button (id=\"sb-opt-icon\"/class \"sb-opt-icon-field\", named to match _quickOptionsHtml()'s own convention so _wireQuickOptions('sb', $popup) wires it with the exact same shared code, no bespoke handler needed); iconValue is now blank whenever the toggle is off, so downstream image/full-image logic and the specialEntry.icon save already handle it correctly unchanged. button.js's injectButtonBackgroundOption() replaces the old checkbox with a matching Background icon button appended into that same row, reusing its already-delegated '.de-config-option' click handler instead of wiring its own (a plain button, unlike the injected Background toggle for Device/Widget Config, since a slide button is only ever created through this one popup - editing an existing one goes through Device Config instead - so there's no prior no_background value to hydrate). Moved the Slide button popup's Display options row (Icon/Background) to the top of the popup body, matching every other quick-add popup's layout (_quickOptionsHtml() always renders it immediately after the modal-body opens, before the popup's own fields) - it had been left after the Name/Key/Title/Screen fields instead, an inconsistency the previous fix didn't catch. Fixed #197: the Bar visual mode's segment labels (js/components/dial.js's updateBar()) hard-coded Open/Closed for the 0%/100% ends regardless of device type, so a Dimmer showed 'OPEN'/'DICHT' instead of On/Off - now branches on the live device's own SwitchType, matching the isDimmer detection make() already uses elsewhere. Built a new, working Slider visual mode for Dimmers - previously Needle/Slider was Blinds-only (the button stayed disabled for a Dimmer in Device Config, and getDimmerBlock() never even checked block.needle): getDimmerBlock() (js/switches.js) now gets the same block.needle === true early-return branch getBlindsBlock() already has, reusing renderBlindsSliderBlock() instead of duplicating it. That function now branches on the device's own SwitchType too - Dimmers get On/Off labels instead of Open/Closed, switchDevice('on'/'off') instead of switchBlinds(), and no Stop button (a dimmer has no motor to stop). addSlider()'s existing top-to-bottom scale mirroring, needed for Blinds' Open-at-top/Closed-at-bottom convention, is skipped for a Dimmer's slider (tagged with a new .dimmer-slider class on its .blinds-slider-wrap) so it keeps jQuery UI's own normal bottom-to-top layout instead - 0% Off at the bottom, 100% On at the top, matching a physical dimmer/volume slider and the On/Off button positions beside it; the .ui-slider-range fill's CSS anchor flips to match (bottom-anchored, growing up, instead of Blinds' top-anchored, growing down) so it still visually tracks the handle. Device Config's supportsNeedle now includes Dimmer (matching supportsBar, which already did), enabling the Slider button for them; Inverse - meaningless for a Dimmer, which always runs a fixed 0% Off to 100% On - stays hidden even though Slider mode now applies to it. Verified with the full node --test suite (150 tests), Prettier's format check, and live Playwright rendering of both Bar and Slider mode against a real Dimmer device (correct On/Off labels, correct button-to-command mapping, correct slider fill direction), alongside a regression check that Blinds Percentage devices' existing Bar/Slider behavior is unchanged. Follow-up to #197: a Dimmer's Slider up/down buttons still looked like Blinds' matched pair of green move-actions, which doesn't read as an On/Off toggle. renderBlindsSliderBlock() now tags the down (Off) button with a .blinds-slider-action-off class for Dimmers, colored red to contrast with the up (On) button's existing green - green On, red Off, like a switch's own on/off colors. Blinds' own up/down buttons are unaffected (still both green, no class added). Second follow-up to #197: the up/down buttons still used fa-chevron-up/fa-chevron-down, which read as a physical move action rather than an On/Off toggle. For Dimmers only, those icons are now fa-toggle-on/fa-toggle-off instead - Blinds keep the chevrons unchanged. Also overrode the global .fas.fa-toggle-off opacity: 0.5 rule (meant for a small inline status icon elsewhere) for this button specifically, so its icon stays fully visible against its red background instead of washing out.",
        "3.45.5": "Updated dayjs (1.11.21 to 1.11.23) and sass (1.101.0 to 1.103.1) to their latest patch releases, following up on the technical audit's dependency-update recommendation. Verified with a clean npm ci, a production build (no new Sass deprecation warnings, entrypoint size unchanged within budget), the full node --test suite, Prettier's format check, and the Playwright suite. Fixed the sunrise/sunset widget showing a real, clickable scrollbar instead of clipping its content on a grid row too short for its header plus the sunrise/sunset line (.sunriseholder now gets the same height/overflow clamp every sibling grid-aware block already had). Added a confirmation prompt (\"Remove this tile from the layout?\") before the Layout Editor's per-tile \"-\" remove button actually deletes a block or widget, matching the existing confirm() pattern already used for screen deletion and Wizard grid conversion. Fixed the Layout Editor showing a Group block's (js/components/group.js, type: 'group') settings control as a plain drag icon instead of the normal configuration cog, so its config popup never opened - _resolveBlock() only recognized separator/html/lms/widget/idx-based-device blocks, so a group block (which has no numeric idx) fell through to the untyped 'grid' fallback kind with no config affordance; same fix shape as #168's HTML block cog icon. Fixed a block's configured custom icon (e.g. a Group block's default fas fa-object-group) silently rendering as a blank/broken image instead of the icon whenever getBlockConfig() had also reset that block's image to '' - iconORimage() (js/blocks.js) checked typeof block['image'] !== 'undefined' rather than its truthiness, so the defined-but-empty '' image always won over a real configured icon since it is checked after the icon check. Replaced Device Config's Data/Updated/Title/No background switches with icon buttons matching the existing Icon/Dial/Bar/Slider visual-mode picker's look (#195), and moved Icon out of that mutually-exclusive Dial/Bar/Slider group into its own independent toggle alongside them - Icon/Data/Updated/Title/Background now sit as one row of bordered icon buttons (fa-image/fa-align-left/fa-clock/fa-heading/fa-fill-drip) beside the still-exclusive Dial/Bar/Slider group, both under the same Display options heading. Every device now gets an Icon toggle (previously only Group/HTML/LMS/Separator specials did, and dial-capable devices only got it as one of the exclusive modes); toggling it shows/hides the icon custom-field row independently of whichever Dial/Bar/Slider mode, if any, is also selected. button.js's injected 'No background' checkbox (#170) is now a matching 'Background' icon button (fas fa-fill-drip) for Device Config specifically, reusing the same active-state styling/click-to-toggle behavior via a shared .de-config-option class while still saving itself independently via its own custom field; Widget Config keeps its original switch for this control, unchanged. Verified with the full node --test suite, Prettier's format check, and the Playwright suite (including two updated end-to-end tests and one new screenshot-verified rendering pass of the popup in both its Dial-active and all-five-buttons states).",
        "3.45.4": "Added a new \"Needle\" visual mode for Blinds Percentage/Blinds Inverted Percentage devices, selectable in the Device Config popup's visual-mode selector alongside the existing Icon/Dial/Bar options (now Icon/Dial/Bar/Needle, block.needle in CONFIG.js). It renders as a continuous vertical slider - a title, a compact OPEN button above the slider, the slider itself (a wide track with a green gradient fill up to a small round handle, a clickable/typeable percentage readout beside it, and a clickable tick scale down its left edge), then DICHT (and STOP, unless hide_stop is set) below it - implemented as a new renderBlindsSliderBlock() in js/switches.js. This is purely additive: Icon mode's own classic thin percentage bar (getBlindsBlock()'s withPercentage branch) is completely unchanged from its original implementation, and the separate Dial widget's existing Dial/Bar modes (js/components/dial.js) are untouched. Needle's scale tick count reads the same barsteps config field (default 10) the Bar dial subtype already exposes as its Steps field in the Device Config popup - that field is now shown for either mode. Clicking a tick, or the live percentage readout itself, jumps straight to that value (typing an exact number is also supported); dragging the handle works the same as before. The slider is correctly given jQuery UI's orientation: 'vertical' and range: 'min' options so the handle position, drag direction and gradient fill are all actually value-driven, rather than only looking that way via CSS. The block escapes the classic 85px default block height (the same way multi_line blocks like graphs already do) while still properly shrinking/growing with a grid screen's own row-driven height, so resizing the tile in the Layout Editor works as expected. Fixed selecting Needle and saving not actually switching to it (the classic bar kept showing instead, and the Needle button lost its highlighted state on reopening the popup): saveblocks.php/configwriter.php only recognize a fixed set of top-level device properties, unlike Dial/Bar's existing type:'dial' - a plain needle:true on the saved entry was silently dropped, the same way Bar's own subtype:'bar' and barsteps already have to ride through custom_fields instead. needle now does the same, verified directly against configwriter_device_block_props() with the PHP CLI, and the Device Config popup now also hydrates options.needle from the live config when reopening, so the button correctly shows as selected again. Made OPEN/DICHT the same (smaller) height as STOP - they were unexpectedly taller because their chevron icon inherited the theme's blanket .fas { font-size: 30px } rule (the codebase's own .fa-small opt-out still resolves to 20px, itself too big here), overriding the button's own 8px text size and dragging its height back up regardless of padding. Gave STOP a light red background/border to set it apart from OPEN/DICHT. Shrunk the slider handle further (20px to 16px) and made width/height !important so it can never end up non-square; also found and fixed jQuery UI's own bundled vertical-orientation theme rule (.ui-slider-vertical .ui-slider-handle) quietly still applying a margin-bottom: -.6em that nothing had overridden yet, offsetting the handle. Handled Domoticz's inverted blinds percentage scale (0% fully open instead of 100%) for Needle mode: auto-detected from the device's SwitchType (the same \"Inverted\" check already used for the OPEN/DICHT command direction), so the green fill and the OPEN/DICHT command direction both flip together for those devices without any configuration needed. Added a new Inverse switch to the Device Config popup, shown only in Needle mode, pre-checked to match the auto-detected value and editable for the rare device that doesn't expose this correctly through Domoticz - once explicitly saved it overrides the auto-detected value from then on. Persisted the same way needle/barsteps already are (via custom_fields, since saveblocks.php/configwriter.php only recognize a fixed set of top-level device properties), verified directly with the PHP CLI, and verified in a rendered preview that a normal device, an auto-detected inverted device, a manually-forced-inverse device and a manually-forced-normal device all fill and send OPEN/DICHT commands correctly and independently of each other. Made OPEN/DICHT exactly twice STOP's height (rather than matching it) using an explicit height (not just padding) so the ratio holds regardless of any other rule's specificity. Fixed the slider handle visually sitting about half its own height too high relative to the tick marks and value bubble: jQuery UI positions the handle by its bottom edge, not its center, while the ticks/bubble both center on their percentage via transform: translateY(-50%) - restored a negative margin-bottom sized to half the handle's height (an earlier fix in this same file had zeroed this out, mistaking the theme's own equivalent default compensation for an unwanted quirk) so the handle's center now lands on the same percentage the ticks/bubble do. Fixed the scale reading -1% for a fully-closed normal device and 101% for a fully-open inverted one - the slider's min was hardcoded to 1 (not 0), so the percent-space math it fed into (tick labels, the value bubble, the click-to-type input) landed just past 0%/100% for a raw Level of 0. Slider min is now 0, matching Domoticz's real Level range - the same range the Bar dial subtype (js/components/dial.js) already uses. That comparison also caught an earlier misstep in this same release: the tick labels/bubble/typed-value had been made to flip their printed number for an inverted device (0% at the top, 100% at the bottom) to match a request read as wanting the numbers mirrored - but the Bar dial subtype never does this (it always shows the device's raw, unconverted Level regardless of an inverted SwitchType), and that's the behavior actually wanted here too. The flip has been removed and the gradient fill's direction (range: 'min'/'max') un-conditioned back to always 'min' as well, so the slider's number, its physical handle position and its fill now always track the device's raw Level directly, transparent at 0% and completely filled at 100%, for every device; the inverted flag now only changes which direction the OPEN/DICHT buttons move the blind, exactly as the classic bar has always treated it. Corrected that comparison itself: checking the Bar dial subtype's actual template (tpl/dialbar.tpl) and CSS showed its segments are always laid out top-to-bottom as 0%, then increasing to 100% - a plain flex column with no reversal - unconditionally, for every device, not only inverted ones. Needle's scale now matches that same fixed 0%-at-the-top/100%-at-the-bottom layout, the opposite of a plain vertical slider's own min-at-bottom/max-at-top convention. Since jQuery UI has no built-in option to flip a vertical slider, every value handed to or read from the widget (its own initial value, the value read back in change/slide callbacks, a clicked tick's target, a typed exact value) is now mirrored around the midpoint of its 0-100 range before reaching jQuery UI and un-mirrored on the way back out, while every displayed number (tick labels, the value bubble, the click-to-type input) stays in plain raw-value space throughout, so a tick's printed label always matches the raw value it jumps to when clicked. The gradient fill direction (range: 'max' instead of 'min') was adjusted to match, keeping transparent-at-0%/filled-at-100% consistent with the new physical layout. Found the actual cause behind the handle repeatedly appearing stuck near the middle of the track regardless of the device's value, and the chevron staying oversized, on the Modern Dark/Liquid Glass Blue/Liquid Glass Grey themes: each carries its own theme-wide `.fas.fa-chevron-up, .fas.fa-chevron-down { font-size: 40px !important; }` and `.ui-slider-handle { top: 50% !important; margin-top: -20px !important; width: 20px !important; height: 40px !important; border-radius: 14px !important; ... }`, written for the horizontal dimmer slider's up/down buttons and handle. Needle's own chevron-size rule had no !important, so the theme's flatly won regardless of its higher selector specificity; Needle's handle rule never touched top/margin-top at all, so the theme's top: 50% applied uncontested - and once top, height and jQuery UI's own dynamic bottom: value% are all specified on the same absolutely-positioned element, the box is over-constrained and bottom is dropped entirely in favor of top, pinning the handle to the track's vertical center no matter what value jQuery UI was actually tracking. Needle's chevron rule now carries !important (its higher specificity then correctly wins the tie), and its handle rule now resets top/margin-top to auto/0 with !important, handing vertical positioning back to jQuery UI's own bottom entirely; border-radius also gained !important so the handle can no longer be squared off by the theme's 14px. Reproduced by loading a theme stylesheet alongside creative.css in a test render (something earlier verification in this same release had not done) and confirmed fixed: handle position, chevron size and handle shape all now track the real value/design regardless of which theme, if any, is active. Made OPEN/DICHT's chevron exactly as tall as its own surrounding text (font-size: inherit, instead of a hardcoded px value) instead of guessing a fixed size. Fixed the round slider handle visually poking out above/below the slider track's own background at the 0%/100% extremes: the handle can overhang up to half its own height past the track's logical 0%/100% edge (needed so its center still lands exactly on the value's position, matching the ticks/bubble), but the track's own visible background previously stopped exactly at that same edge, cutting the handle off there. The track element's own box (what jQuery UI and this file's own tick/bubble math measure 0%-100% against) is now transparent/borderless and left completely alone, with its actual visible background moved onto a ::before pseudo-element sized 8px (half the handle's 16px) taller at both ends, so the handle now always renders fully inside the visible track. Adjusted OPEN/DICHT/STOP's heights: STOP is 5px taller (20px to 25px) and OPEN/DICHT are 5px shorter (40px to 35px). The tick in the 0%-100% scale closest to the slider's current value is now highlighted (same green the value bubble used to use), so the current position stays visible in the list of percentages. Removed the value bubble entirely (and, with it, the click-to-type-an-exact-value input it offered), leaving the highlighted tick as the slider's only on-track indicator of the current reading - also made that tick's label bold, not just colored, so it still stands out on its own now that it's the sole indicator. Doubled the slider track's width (30px to 60px). Fixed the gradient fill leaving a visibly empty, unfilled sliver at the top of the track's background (and, symmetrically, the bottom) instead of reaching all the way to its edges. The track's own visible background is drawn 8px taller at each end than the coordinate box jQuery UI positions everything against, reserving room for the round handle's own overhang there (see the CSS fix a few releases back in this same entry) - but the gradient fill (.ui-slider-range) is a jQuery UI-managed element sized purely as a percentage of that *unextended* coordinate box, so it always stopped 8px short of the background's actual top, and, for the same reason, never reached down to the handle's own bottom edge at high values either. The fill's anchored top edge (fixed by jQuery UI's own theme CSS, not inline) is now shifted up 8px to match; its height (which jQuery UI does set inline, per value) is topped up by 16px on every value change (drag, tick click, typed - well, every value change now that typing is gone) so its bottom edge always reaches exactly to the handle's own visible bottom edge instead of stopping at the handle's center, matching correctly at every value, not only the 0%/100% extremes. Moved OPEN/STOP/DICHT off their own row above/below the slider and onto a column beside it instead - OPEN top, STOP middle, DICHT bottom, all within the slider's own height (a plain flex column with justify-content: space-between, stretched to match the slider's height via the new row container's align-items: stretch) - freeing up the vertical room they used to take for the slider and its percentage scale instead. The buttons are now small round icon-only buttons (OPEN/DICHT's existing chevrons, plus a new fa-stop-circle icon for STOP, the same icon already used for a media player's own Stop button elsewhere in this codebase) - their OPEN/DICHT/STOP text is gone from the visible button entirely, kept only as an aria-label for screen readers. Moved the button column closer to the slider (10px gap to 5px) and made the buttons themselves 2px bigger (30px to 32px). Shrunk each tick's dash line by half (20px to 10px) and moved its percentage label in to match, keeping the same small gap between them instead of leaving a floating space where the dash used to reach. Moved the button column right up against the slider (5px gap to 1px). Gave DICHT the same green colors as OPEN, instead of its own plain grey. Fixed the percentage scale's ticks (and the track itself) being able to render partly outside a narrower block, and the gap to the button column still looking large even at 1px. Both .slider-scale (the ticks) and .slider (the track) used to center themselves independently within .blinds-slider-wrap via position: absolute plus a fixed negative margin-left each - correct only for the exact wrap width those margins were tuned against, so a narrower block (or the track's own width changing, as below) could push the ticks partly outside the visible block, and a wide wrap left a lot of empty, un-styled space between the visible track and the button column that the wrap's own tiny gap never accounted for. Both are now plain flex children of .blinds-slider-wrap (now itself a flex row) instead, always staying inside it; justify-content: flex-end keeps the scale+track group hugging the button column with no leftover space in between, at any width. The track's own width is no longer a fixed 60px either - it now flexes with how much room the block actually has, so a narrow block shrinks it instead of overflowing or clipping. Removed that width's own upper cap (previously 60px) - it had only ever shrunk along with a narrower block, not grown along with a wider one, since .slider-scale (the only other item sharing the row) never grows, so all of the wrap's own extra width was going unused instead of into the track; a 30px floor is kept so it never disappears on a very narrow block. Also nudged the button column's gap to the slider from 1px to 3px. Made the percentage scale's tick labels 2 sizes bigger, converting font-size from a relative 0.85em (9.35px computed, against an 11px inherited base) to a flat 11px. Renamed the Needle visual-mode button in the Device Config popup to Slider (lang/*.json's dial_needle, and its mention in dial_barsteps_help) - the internal mode/property name (block.needle) is unchanged, only the label shown to the user. Fixed the Inverse switch (Device Config popup, Slider mode) rendering noticeably smaller than the Data/Update switches next to it - it fell outside .de-config-options, the container the Data/Update switches' own bigger size (38x20px instead of Bootstrap's default) is scoped to. Rather than adding yet another one-off selector to that already-long list (css/creative.css already lists five: .de-config-options, #hb-device-border, .we-widget-field, #we-cfg-ascending, .we-block-option, .de-lms-switch), added a new shared .de-switch class carrying the same size/color and applied it to the Inverse checkbox - any future standalone Device Config switch can just add this class instead of needing its own new CSS rule.",
        "3.45.3": "Make the Modern Dark/Liquid Glass Blue/Liquid Glass Grey themes' icon and image size (previously a hardcoded 35px) configurable from the settings menu, next to the existing Font size fields - added a new --icon-image-size CSS variable that the theme files' four icon-size rules (.blocktitle img, .col-icon img/.icon img, .titlegroups .col-icon img.icon, and the dimmer/blinds-slider carve-out) now reference via var(--icon-image-size, 35px) instead of a literal 35px, wired into the same generic CSS-variable settings panel, save endpoint (js/savecustomcss.php) and custom.css override mechanism the Colors and Font size sections already use - no new plumbing needed beyond adding the var name to those existing allowlists/arrays. Also added a new .settings-theme-compact-grid CSS class and put both the Font size and the new Icon size rows in it: those fields were previously full-width single-column rows using the same 40ch input as every other setting, far wider than a short pixel value like \"18px\" needs; the new grid mirrors the existing Colors section's already-compact 2-column layout, and the input itself is now capped at 110px instead of 40ch. Split the single --icon-image-size variable into two independent ones - --icon-font-size (the FontAwesome icon column, .col-icon .icon) and --icon-image-size (actual img icons) - since a device's custom image and a widget's FontAwesome icon are unrelated and were previously forced to the same size; the Icon size section now shows an Icon row and an Image row. Also changed the Font size and Icon size fields from free-text (requiring e.g. 18px to be typed) to a bare number with a fixed px suffix shown next to the field - these variables are always a pixel size, so there was never a reason to type or accept a unit. Moved the Theme tab's background-image preview swatch and the active custom stylesheet notice to sit beside their Choose background image / Path/URL fields instead of stacking underneath them - renderBackgroundPicker()'s two rows now lay out their control column as a flex row instead of the default block stacking, and get a wider control column than a plain field to fit both side by side. Widened the active custom stylesheet notice next to the Path/URL field - it was sizing to its own content by default, wrapping its text across 4 short lines; it now has a 260px minimum width and grows to fill the row, wrapping across 2 lines instead. Removed the Media tile from the settings menu (switch_horizon, host_nzbget and hide_mediaplayer) - it saw little use as a dedicated settings category, and the config keys it edited are still fully functional (js/switches.js, js/components/nzbget.js, js/blocktypes.js/blocks.js) for anyone who sets them directly in CONFIG.js; only the settings-UI entry point was removed. Fixed CONFIG.js accumulating growing runs of blank lines between editor-managed sections (screens, grid layouts, widgets) every time one was resaved. configwriter_remove_section() spliced the raw text before the removed section straight onto the raw text after it, but both sides already carried their own leading blank line from configwriter_wrap_section() - so every removal (which happens on every save, since a section is always removed and re-appended) stacked another blank line onto the same spot, compounding without bound over repeated saves. It now trims the whitespace on both sides of the cut and rejoins with exactly one blank line. Also fixed configwriter_upsert_root_config_settings() (config[\"key\"] = value; settings written by the Settings UI) always deleting and re-appending a changed key at the end of the config block, scattering it away from the related settings it was originally grouped near - an existing single-line setting is now updated in place instead, only genuinely new keys get appended. Centered a Selector Switch device's option buttons (.btn-group.selector-buttons, e.g. Open/Half/Dicht) horizontally within its block on the Modern Dark, Liquid Glass Blue and Liquid Glass Grey themes, instead of hugging the right edge. Bootstrap's .btn-group is an inline-flex element, so it inherited its position from .mh's text-align: right (used to right-align plain value text elsewhere in the same block); the button group now gets its own display: flex; justify-content: center; width: 100%, unaffected by that text-align. Vertically centered a Selector Switch's option buttons/dropdown below the title, and gave the SelectorStyle 1 dropdown (e.g. Husqvarna, Lyrion) the same full-width treatment the buttons already got. .col-data is now a flex column - scoped with :has(.selector-buttons)/:has(> select) so every other block's plain title/value/lastupdate stacking is untouched - with the button group or select centered in the remaining space via auto margins. Fixed editing a device's config from inside the Layout Editor, clicking OK, then editing a different device and clicking OK, silently reverting the first device's change - only the most-recently-edited device's edit actually persisted. openLayoutConfig() reopens the Device Config popup once per edited device without a page reload in between (an in-progress drag/resize in the Layout Editor must survive it), and each confirmed change is persisted immediately via _saveDeviceConfigOnly(), which resends every currently-placed device's definition, not just the one being edited. But _init()/_prepareManagedDeviceState() unconditionally rebuilt every device's in-memory state from blocks[]/columns[] on every popup open - and those client-side globals are never patched after a save, only the server's CONFIG.js is - so the second edit's save resent the first device using its stale pre-edit data, reverting it server-side. Reproduced directly via saveblocks.php's actual request payloads (second request's first device reverted to its original title) and fixed: openLayoutConfig() now preserves already-known device/special state across repeated calls in the same session instead of re-deriving it from the stale client-side globals; verified the same reproduction now correctly resends both devices' edited titles. Fixed finger-swipe screen navigation silently doing nothing on narrow (phone-width) touch devices when the 'Enable Swiper' setting was set to 1. Per its own settings help text (lang/*.json's enable_swiper_help), 1 means \"Enable on narrow screens\", but buildSwipingScrolling() (js/main.js) tested !vertical_screen for that value - the opposite - so it only ever started Swiper on WIDE screens and skipped it entirely on narrow ones. With Swiper never created, screenswitcher.js's non-swiper goToScreen() fallback only show()/hide()s the active screen and has no gesture handling at all, so a finger swipe had nothing listening for it. Flipped the condition to match the documented behavior. Fixed touch-swipe screen navigation not working at all on at least one real Android tablet, even with 'Enable Swiper' left on its default value - the screen-switcher buttons and mouse drag both still worked there, isolating it to something Swiper-specific. Swiper was being loaded via a lazy import() chunk (window.loadSwiper() in src/index.js), separate from the main dist/bundle.js - on that tablet, fetching that separate chunk at runtime silently failed, so myswiper never got created and no touch handler was ever listening for a swipe (confirmed by comparing against the master branch, which bundles Swiper directly and doesn't have this problem). Swiper is now imported statically (import Swiper from 'swiper/bundle') straight into dist/bundle.js, removing the separate runtime fetch entirely; bumped the loader.js/index.html cache-bust counter so already-loaded tablets actually pick up the rebuilt bundle instead of a stale cached copy. Made the free-positioned grid layout's gridColumns (default 24) and rowHeight (default 20px) configurable dashboard-wide from Settings > Weergave (screen), instead of only per-screen in CONFIG.js - useful for tuning the grid's precision for a specific device's real usable viewport. Any screen using layout: 'grid' that doesn't set its own gridColumns/rowHeight now falls back to these settings before the hardcoded 24/20 default, so leaving both untouched keeps every existing install's grid screens exactly as they were; changing either after blocks were already placed will require repositioning them, same as changing a screen's own gridColumns/rowHeight always has. Also corrected docs/screens.rst, which still documented the grid layout's rowHeight default as the old value of 40 (and used it in both its example snippets) - the code's actual current default has been 20 for a while, with an existing migration shim that already treats an explicit rowHeight: 40 on a screen as the legacy value and normalizes it to today's real default rather than an intentional 40px choice. Fixed the new Settings > Weergave gridColumns/rowHeight defaults having no effect on any screen that had already been saved once through the Layout Editor - which, in practice, was every real grid screen. Every save (a plain drag/resize, a fresh columns-to-grid conversion, or adding a device/widget tile via those editors) unconditionally pinned an explicit gridColumns/rowHeight onto that screen, snapshotting whatever was in effect at that moment - so the dashboard-wide setting could never reach a screen that already had its own value, silently shadowing it. Save flows now only pin an explicit value when it actually diverges from the dashboard-wide default; a screen that matches it goes back to following the setting, and future changes to it apply immediately with no further save needed. Verified live: a screen with matching values had its explicit gridColumns/rowHeight dropped on a plain re-save, and afterwards picked up a changed global rowHeight on page load with no editor interaction at all. Changing the dashboard-wide gridColumns/rowHeight default in Settings > Weergave now also clears any existing per-screen gridColumns/rowHeight override for that same property, on every screen at once. Without this, an install with existing grid screens (every one of them, previously, since every save used to pin an explicit value) could change the setting from the menu and see absolutely nothing happen anywhere, since a per-screen value still always takes priority - matching the setting is not enough to also retroactively reach screens saved before that fix landed. Only the specific property actually changed in that save gets cleared - changing rowHeight alone leaves an existing gridColumns override untouched, verified live alongside the resulting resolved CSS grid variables on the affected screen. Moved a Selector Switch's option buttons/dropdown 5px above dead-center within its block, shrinking the gap to the title above by 5px (and growing the gap below by the same amount), on the Modern Dark, Liquid Glass Blue and Liquid Glass Grey themes. Applied as a translateY(-5px) on top of the existing margin: auto centering, so it's a flat 5px shift regardless of the block's height rather than a fixed off-center position. Fixed unreadable white text on the Device Config popup's Icon/Dial/Bar display-mode buttons when selected. Bootstrap's default .btn-outline-secondary.active text color (white) assumes the usual solid dark active background, but this component overrides that background to a light mint green - white text on white-ish background was effectively invisible. The selected button's label now uses the same accent blue already applied to its icon. Shifted a section title block's (type: 'blocktitle', e.g. 'Lichtschakelaars') title text 10px to the right of its icon, on the Modern Dark, Liquid Glass Blue and Liquid Glass Grey themes, via a translateX(10px) on .blocktitle .dt_title.",
        "3.45.2": "Fix thermostat (and other) dial widgets still rendering off-centre on the Modern Dark, Liquid Glass Blue and Liquid Glass Grey themes - the earlier #177 fix only zeroed the dial wrapper's padding, but the themes' generic panel styling (padding/border/background/box-shadow) still applied to it and could still shift the dial's square-face calculation off-centre; dial components (.transbg.dial) are now fully excluded from that themed panel styling, so they use the same plain, unthemed layout as the default theme, where the dial was already correctly centred (#177); add a Full-width image toggle to the Add Button Wizard popup's Icon/Image picker - it previously only ever saved a picked custom image as the small, fixed-size icon field, with no way to reproduce a webcam/radar-style button (btnimage) that fills and scales with the block, so users had to resort to an oversized icon floating in an otherwise-empty block (#171); update 4 tests/source.test.js assertions left stale by the Bar subtype work (#182), which moved the Dial checkbox into a shared Icon/Dial/Bar visual-mode selector without updating the tests pinning its previous single-checkbox shape - no production code changed, the tests now assert deviceeditor.js's actual current implementation; fix a Full-width image (or plain URL/popup) button always rendering with a permanent bluish-tinted 'active menu button' background instead of matching every other block's default background - every button created via the Add Button Wizard carries a slide property so it's still recognised as a button even without an image, which also always tagged it with the .slide/.slideN CSS classes js/main.js uses to highlight the currently active screen's menu button; buttons whose real action is a URL/popup (newwindow set) no longer get those classes, since they never actually navigate via slide, while genuine slide/menu buttons keep highlighting correctly (#171); fix the Bar subtype (#182) losing its themed panel background/border/shadow on Modern Dark, Liquid Glass Blue and Liquid Glass Grey - the #177 fix excluded every .dial element from that styling to fix the circular dial's centering, but Bar is a vertical rectangle sharing no part of that centering problem (dial.js's _dialFitSize() already measures its content box directly), so it now gets the same themed panel every other block has, using the .dialbar class dial.js already adds to its parent to tell it apart from the circular dial (#182); make the Bar's number of segments configurable via a new barsteps block parameter (default 10, e.g. barsteps: 5 gives 5 segments of 20% each plus the 0% segment) instead of always being fixed, and add a Steps number input to the Device Config popup's Icon/Dial/Bar selector (shown once Bar is chosen) so it no longer has to be set by hand via Custom fields; the selected option in that Icon/Dial/Bar selector now uses the same light-green 'added' look as a selected widget card, with a blue icon instead of matching the border color; update 6 tests/php-security.test.js and tests/source.test.js assertions left stale by earlier LMS artwork-retry and cover-artwork-path work - ; give Device, Multi Device and Custom device blocks on the classic (non-widget) rendering path the same 45px icon column width as every other block type placed on screen (Widget, Separator, Slide button, Group) - Widget/Separator/Slide button/Group build their wrapper through dashticz.js's getContainer(), which already includes the dt_block class the wider .dt_block .col-icon rule (45px) targets, but plain devices and Multi/Custom device value rows built their wrapper without that class and so fell back to the narrower bare .col-icon rule (40px); js/components/domoticzblock.js's placeholder wrapper (used by every plain single-idx device) and js/blocks.js's ; fix custom image icons (and inline dial value-row icons) rendering far too large on the Modern Dark, Liquid Glass Blue and Liquid Glass Grey themes - those themes' blanket .icon { font-size: 40px !important; } rule matched any element carrying the generic icon class, not just the intended main device/widget icon column, blowing up unrelated inline icons (e.g. a thermostat dial's inline value-row icon); their .col-icon img, .icon img { max-width: 65px !important; } rule was more than double the 30px every other theme and block type uses, so a device's custom image icon (e.g. a Custom/Multi device with an image: field) rendered visibly larger than a Separator's or Widget's icon using the very same file. The font-size rule is now scoped to .col-icon .icon (leaving inline icons elsewhere alone) and the image cap is now 30px, matching the default theme and the themes' own existing dimmer/blinds-slider carve-out; unify all image-icon sizing across those three themes to 35px (previously 30px for the general col-icon/icon image cap and the dimmer/blinds-slider carve-out, 34px for the Separator's .titlegroups .col-icon img.icon and .blocktitle img rules) so a device's image icon and a Separator's image icon render at the exact same size instead of two slightly different ones; tune the Swiper config (js/main.js's startSwiper()) to fix screen navigation (both swiping and tapping a slide button) being unreliable on tablets while working fine on a PC - a touchscreen tap almost always drifts a few px, unlike a precise mouse click, and Swiper's default threshold (5px) misreads that drift as an aborted swipe attempt; while animating, its default preventClicksPropagation then stops that tap from ever reaching the block's click handler, which never shows up on desktop since a mouse click rarely moves at all. threshold is now 10 and preventClicksPropagation is now false; fix js/loader.js's loadScript() (used for js/main.js, js/functions.js and js/polyfills.js) serving a stale cached copy of those files after any same-day edit, with no visible sign anything was wrong - it busted the cache on the static _DASHTICZ_VERSION build number, which is only bumped on a real dist/bundle.js rebuild, so a device that had already loaded the dashboard earlier that day (e.g. a tablet left on) kept serving its old cached js/main.js indefinitely, silently missing the Swiper tuning above. It now busts on a per-page-load timestamp instead, matching how the theme CSS already cache-busts; dist/bundle.js itself is intentionally left on _DASHTICZ_VERSION",
        "3.45.1": "Fix the No background checkbox in Device Config and Widget Config rendering smaller than, and separately positioned from, the other Display options switches (Icon/Data/Title) - it now sits inside the same switch row and matches their size, color and spacing exactly, always sharing a single row with them (including when the Dial checkbox dynamically hides/shows Icon and Title) instead of landing on a row by itself; also fix enabling it leaving a soft colored glow behind the block on the Liquid Glass themes instead of true transparency - their backdrop-filter blur/saturate effect kept sampling whatever sits behind the block and is now cleared too (#170); add a Hide block when player is off switch to the Lyrion Music Server Wizard popup, showing nothing at all (no icon, no text) instead of the usual 'Player off' message while the player is powered down, sized and styled to match the other Wizard switches; update two stale test assertions left behind by an earlier basicclock.js v4 sizing fix (#175) to match its actual current behavior; add a new Dial subtype 'bar' for Blinds Percentage/Blinds Inverted Percentage devices, rendering them as a vertical 10-segment bar (0% at the bottom, 100% at the top) where tapping a segment sets the device directly to that segment's 10% level, with a Bar switch in the Device Config popup (shown once Dial is enabled on a qualifying device) so it no longer has to be set by hand via Custom fields; fix the bar's segments having no height at all on grid screens by having js/components/dial.js's _dialFitSize() measure .dt_block's own content height and set .dt_content to match (it previously only got height:100% via the .fixedheight class, which grid items never get), so the bar now truly scales - both height and width - with the block like the classic circular dial already did, while leaving it unset below the mobile-stack breakpoint where a grid item's height is itself content-driven (forcing it there fed back into its own measurement and grew without bound); the bar's title now uses the same .dt_title styling every other device/widget title uses instead of its own bar-specific size/color, and its bottom padding is a fixed 10px below the percentage value instead of a large leftover gap",
        "3.45.0": "Give every Screen Editor quick-add popup (Custom device, Multi Device, Group, HTML Block, Slide button) the same Icon/Image row as Device and Widget Config, with a Display options heading to match; a picked custom image now saves as a proper image custom field instead of being forced through the icon slot; fix a Separator with a custom image also rendering its old default icon next to it; right-align Slide button titles and size a Slide button's custom image the same as other devices' images in the Modern Dark/Liquid Glass themes; the Device Config popup title now shows the device's IDX in brackets for unambiguous identification; fix the Layout Editor showing an HTML block's settings control as a generic drag icon instead of the normal configuration cog, so its config never opened (#168); fix a Separator/title block with no 'icon' property at all (legacy/Custom mode config) rendering a default divide icon instead of no icon, matching Wizard's explicit icon-off state (#169)",
        "3.44.3": "Keep the Layout Editor open when switching screens (each screen keeps its own pending edits) and when adding a device/widget/separator via the Add items menu (added as a pending tile instead of saving and reloading immediately); fix a 'Grid block is not declared' error when saving a newly added tile on a grid screen",
        "3.44.2": "Fix the Layout Editor's per-tile gear icon surfacing the full Device Editor instead of returning to the grid; its Device Config popup now saves the block directly (blocksOnly) and leaves layout/grid position data untouched",
        "3.44.1": "Move the Icon/Update/Title checkboxes to the top of every Screen Editor quick-add popup (Custom device, Multi Device, Group, HTML Block); add a matching-height Back button to every popup reachable from the add-item tile menu (the 5 quick-add popups plus Add device and Widgets); and hide the now-nonfunctional wand/Screen Editor topbar icon in favor of the + icon while editing, restoring it when editing closes",
        "3.44.0": "Add Group and HTML Block to the Screen Editor's add-item menu, and give Multi Device's own popup the Icon/Update/Title options every other quick-add popup now shares",
        "3.43.5": "Fix Hayman clock rendering oversized and its separator dots sitting too high above the digits",
        "3.43.4": "Fix Flip clock leaving empty space below the digits, Hayman clock's separator dots being misaligned/not scaling, and Miniclock's text not resizing with its block",
        "3.43.3": "Fix Basic clock's oversized margins and make all four clock types live-resize with the grid editor, without runaway growth or Flip/Hayman rendering glitches",
        "3.43.2": "Clock widgets always scale to fill their block; remove the Size (px) field (Scale-only) and add a preview image to the Clock type picker",
        "3.43.1": "Change the Separator/title block's default icon and its Screen Editor add-menu tile icon to fa-divide",
        "3.43.0": "Refine Widget and Device configuration, fix Dial state and sizing, and improve compact grid blocks",
        "3.42.10": "Restore classic-column Dial sizing and preserve iconless legacy iframe and Sunrise/Sunset configurations",
        "3.42.9": "Secure Domoticz log rendering, prevent repeated log listeners, fix Group toggleoff commands, update build dependencies, and clean stale build output",
        "3.42.8": "Fix Settings save sending an empty payload (theme and other settings silently reverted after save) caused by a merge conflict resolution splitting the collected values into an unused local object",
        "3.42.7": "Recognize 'insufficient permission'/'failed to write object' Git errors in the web Update button so a concrete chown/permission-fix hint is shown instead of a raw Git error",
        "3.42.6": "Add Liquid Glass Grey and Liquid Glass Blue themes, based on Modern Dark with the same block rounding/spacing, restyled as frosted translucent glass panels",
        "3.42.5": "Fix missing News widget icon; fix Sunrise/Sunset icon/title (default icon, compact header row, correct stacking on grid screens); Custom/Multi Device creation offers an Updated checkbox instead of silently defaulting it off, and the Icon checkbox now actually persists when turned off",
        "3.42.4": "Fix invisible multi-camera thumbnails on grid screens (#132); Dial checkbox on a multi-value device (e.g. Temp+Humidity) now saves the base idx instead of a sub-value idx (#118)",
        "3.42.3": "Fix Device Editor reintroducing a cleared iframe/widget height on a grid screen (#100 follow-up)",
        "3.42.2": "Fix Domoticz log widget triggering a spurious outer scrollbar on grid screens (#105)",
        "3.42.1": "Fix Device Editor saves reintroducing a default widget height on grid screens (iframe/camera); fix Domoticz Security Panel device rendering as an empty tile",
        "3.42.0": "Device Editor: add a Dial checkbox to Device Config; fix dial blocks rendering oversized by default and showing scrollbars on grid screens; live-sync dial sizing with editor resize; tighten roomy default dial margins; document the dial scale parameter",
        "3.41.7": "Fix TimeGraph config icon opening the wrong device settings; add default icons for Multi Device and Custom Device",
        "3.41.6": "Domoticz log widget now defaults to an 8x8 grid cell instead of a full-width strip",
        "3.41.5": "Device Editor: add a Title checkbox to Device Config, covering plain devices, the separator/title bar (Tussenbalk), Slide button, Multi Device and Custom device",
        "3.41.4": "Fix clock widgets (Basic, Flip, Station, Hayman) ignoring the Title checkbox/hide_title setting",
        "3.41.3": "Fix stale $forceClone test assertion and remove the duplicate tests/phpsecurity.test.js file",
        "3.41.2": "Radio widget: single Add-station control; grid layout: lower the minimum block height to 2 rows and fix Sunrise/Sunset not keeping a resized grid height",
        "3.41.1": "Add Domoticz log, OWM, Sunrise/Sunset and Timegraph special widgets to the Widget Config editor",
        "3.41.0": "Replace the Air Quality widget's Longfonds/RIVM provider with WAQI; fix the missing icon on the Public transport widget",
        "3.40.7": "Fix Device Editor still 400-ing on a globally-configured XMLTV widget: the resubmitted URL only checked the block, not the xmltv_url setting",
        "3.40.6": "Add an Icon option to the Separator/title bar block, instead of rejecting 'icon' as a reserved custom field",
        "3.40.5": "Remove leftover dead alignment-editor helper and fix a stale grid-layout test assertion",
        "3.40.4": "Fix Device Editor save failing with an XMLTV/iFrame URL error whenever an unrelated device was added",
        "3.40.3": "Fix grid screens: widgets no longer get a default fixed pixel height that fights the grid row sizing",
        "3.40.2": "Fix grid screen: same block on multiple screens now keeps independent per-screen positions",
        "3.40.1": "Fix theme panel: only read CSS overrides from saved custom.css block, not from theme stylesheets",
        "3.40.0": "Wizard editor: device/widget config cogs, named config headers, new default background and alignment cleanup",
        "3.30.3": "Screen Editor: add direct separator and dedicated Custom devices popup",
        "3.30.2": "Fix theme reset option staying in dropdown after selecting original theme",
        "3.30.1": "Add Theme settings panel with CSS variable overrides; warn before switching to Wizard mode",
        "3.30.0": "Dashticz cumulative cleanup and fixes",
        "3.23.7": "Fix topbar block order so custom bars render blocks in config order",
        "3.23.6": "Store XMLTV widget editor settings in global config entries for consistency with other widgets",
        "3.23.5": "Fix XMLTV widget URL not saved or read back when changed in grid-mode widget editor settings",
        "3.23.4": "Fix the XMLTV widget so it keeps its own source/settings and supports cached XML, GZ and ZIP feeds",
        "3.23.3": "Use bundled Standby/One/Two/Three/Four PNG icons for the screen switcher in custom topbar mode",
        "3.23.1": "Add topbar_use_png_icons setting to switch editor icons between Font Awesome and img/icons/ PNG files",
        "3.23.0": "Add per-screen custom icons in topbar screen-switcher; XMLTV TV Guide widget; i18n for all screenswitcher labels",
        "3.22.2": "Auto-detect custom mode when config_mode is absent from CONFIG.js",
        "3.22.1": "Always show editor delete button; calendar agenda auto-expands to fit content",
        "3.22.0": "Preserve widget settings and add Standby grid parity",
        "3.21.7": "Make Wizard convert column screens to compact free-positioned grids",
        "3.21.6": "Allow grid blocks to be dragged into arbitrary empty rows",
        "3.21.5": "Add drag-and-resize editing and persistence for grid screens",
        "3.21.4": "Add optional free-positioned CSS Grid screens with mobile stacking",
        "3.21.3": "Use compact widget defaults, multi-camera configuration, and stable layout saves",
        "3.21.2": "Keep CONFIG.js settings, blocks, columns, and screens grouped after editor saves",
        "3.21.1": "Restore large Modern Dark selector buttons and document the complete browser editors and installer",
        "3.21.0": "Background BG_ pickers, clock fit-to-tile, topbar logo, optional topbar clock, git update safe.directory",
        "3.20.5": "Blocks configuration editor with block sizing",
        "3.20.4": "Device Editor: generate named blocks, grouped columns, and proper screens[1] in CONFIG.js",
        "3.20.3": "Added Docker Alpine+nginx+PHP8.3 image and docker-compose setup (jgaalen)",
        "3.20.2": "Doc fix: thermostat.js reference updated to tempcontrol.js (jgaalen)",
        "3.20.1": "Added HVC waste collection provider (me-processware)",
        "3.20.0": "MadPatrick fork: modernized stable release with Bootstrap 5 compatibility",
        "3.14.2.0": "Fix for Domoticz connection",
        "3.14.1.4": "Fix for Logitech Media Server",
        "3.14.1.3": "Fix for default switch block",
        "3.14.1.2": "Playwright test",
        "3.14.1.1": "Domoticz block bug fixing",
        "3.14.1": "Domoticz block enhancements",
        "3.14.0": "Beta version same as 3.14 master",
        "3.14": "Master version derived from 3.13.1 beta",
        "3.22.3": "Translate widget catalog and editor-icon tooltips using the active language file",
        "3.22.4": "Widget titles always reflect the active language, ignoring any hardcoded title in CONFIG.js",
        "3.22.5": "Device Editor: add Domoticz groups and scenes to the device list",
        "3.22.6": "Device Editor: sort available devices as Groups, Scenes, then Devices",
        "3.23.2": "Fix topbar custom icon selector: rename to Custom iconen, invert logic so ON=custom PNG and OFF=Font Awesome"
    }
}
